# Don't copy node_modules into the image (they're macOS binaries)
**/node_modules

# Don't copy git history into the image
**/.git

# Don't copy environment secrets into the image
.env
.env.*
!.env.example

# OS files
.DS_Store
**/.DS_Store
